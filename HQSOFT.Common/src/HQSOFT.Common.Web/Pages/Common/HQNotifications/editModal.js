var abp = abp || {};

abp.modals.hQNotificationEdit = function () {
    var initModal = function (publicApi, args) {
        var l = abp.localization.getResource("Common");
        
        
        
        
        
    };

    return {
        initModal: initModal
    };
};
